import { FilterDisplayDirective } from './filter-display.directive';

describe('FilterDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new FilterDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
