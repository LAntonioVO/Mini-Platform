// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.





export const environment = {
  production: false,
  // Your web app's Firebase configuration
  firebaseConfig:{
    apiKey: "AIzaSyCHDC7fRVGeCTN0ObegygwqIyY-bv_LFlo",
    authDomain: "bts-mini-platform.firebaseapp.com",
    databaseURL: "https://bts-mini-platform.firebaseio.com",
    projectId: "bts-mini-platform",
    storageBucket: "bts-mini-platform.appspot.com",
    messagingSenderId: "496973552475",
    appId: "1:496973552475:web:f58f75107a2357135a187e"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
